---
title: 客户端 SDK
description: 游戏客户端调用的 @apollokit/client,含 HMAC 签名
icon: Gamepad2
---

> 这是 **游戏客户端** SDK (`@apollokit/client`)。如果你在找 **管理端** SDK(用于构建内部工具),请看 [sdk](./sdk)。

占位:详细内容将在下一次提交里补上。本页最终会覆盖:

- `@apollokit/client` 安装与初始化
- 通过 `openapi-ts` 从 `/openapi.json` 重新生成类型
- Client Credentials + HMAC 签名示例(TS / cURL / Python / Go)
- 端到端调用示例
- TypeScript 之外语言:C# / Go / Python 计划中
