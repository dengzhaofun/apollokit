---
title: 错误与排查
description: 统一错误响应、错误码清单、requestId 的排查用法
icon: AlertTriangle
---

占位:详细内容将在下一次提交里补上。本页最终会覆盖:

- 统一响应体 `{ error, code, requestId }` 的每个字段语义
- `ModuleError` 到 HTTP status code 的映射表
- 各模块常见错误码汇总
- 如何用 `requestId` 在 Worker 日志里定位问题
